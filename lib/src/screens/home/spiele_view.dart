import 'package:flutter/material.dart';

class Spiele extends StatelessWidget {
  const Spiele({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: const Text('Spiele'),
    );
  }
}